# Devin Configuration
# Devin reads AGENTS.md from project root
# Rules are inlined into AGENTS.md (Devin has no separate rules directory)
name: "Devin"
enabled: true

targets:
  agents:
    dest: "AGENTS.md"

  rules:
    inline_into_agents: true

  skills:
    dest: ".devin/skills"
