# Google Antigravity Configuration
name: "Google Antigravity"
enabled: false

targets:
  agents:
    dest: ".agent/AGENTS.md"

  rules:
    dest: ".agent/rules"
    header: "---\ntrigger: always_on\n---"

  skills:
    dest: ".agent/skills"
