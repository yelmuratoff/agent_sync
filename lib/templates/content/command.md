---
description: >-
  What this command does — shown in the command list.
argument-hint: "<optional-arg>"
---

[Prompt body. Describe what the AI should do when this command is invoked.]

Tips:

- `$ARGUMENTS` is replaced with the text the user types after the command name.
- `` !`shell command` `` runs a shell command at invocation time and embeds its output into the prompt.
- Keep the command focused on a single workflow.
