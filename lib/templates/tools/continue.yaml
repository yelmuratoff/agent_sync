name: "Continue"
enabled: false

targets:
  agents:
    dest: ".continue/rules/00-context.md"
  rules:
    dest: ".continue/rules"
  skills:
    inline_into_agents: true

  settings:
    source: ".ai/src/settings/continue.yaml"
    dest: ".continue/config.yaml"
