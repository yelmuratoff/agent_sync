name: "Continue"
enabled: false

targets:
  rules:
    dest: ".continuerules"
    merge_to_file: true
    prepend_agents: true
  skills:
    inline_into_agents: true

  settings:
    source: ".ai/src/settings/continue.yaml"
    dest: ".continue/config.yaml"
