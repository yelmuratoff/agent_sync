name: "Aider"
enabled: false

targets:
  rules:
    dest: "CONVENTIONS.md"
    merge_to_file: true
    prepend_agents: true
  skills:
    inline_into_agents: true

  settings:
    source: ".ai/src/settings/aider.yaml"
    dest: ".aider.conf.yml"
