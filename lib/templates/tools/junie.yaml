name: "JetBrains Junie"
enabled: false

targets:
  agents:
    dest: ".junie/AGENTS.md"
  rules:
    inline_into_agents: true
  skills:
    dest: ".junie/skills"
  commands:
    dest: ".junie/commands"
  subagents:
    dest: ".junie/agents"
  mcp:
    source: ".ai/src/mcp/junie.json"
    dest: ".junie/mcp/mcp.json"
