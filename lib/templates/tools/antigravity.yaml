name: "Google Antigravity"
enabled: false

targets:
  agents:
    dest: "GEMINI.md"

  rules:
    dest: ".agents/rules"
    header: "---\ntrigger: always_on\n---"

  skills:
    dest: ".agents/skills"

  commands:
    dest: ".agents/workflows"
