# Codex project config — .codex/config.toml
# https://developers.openai.com/codex/config-basic
#
# Add MCP servers through `.ai/src/mcp.json` or a per-tool Codex MCP source.
# AgentSync composes them into this file at sync time. Direct [mcp_servers]
# settings are supported only when no separate MCP source is selected.
