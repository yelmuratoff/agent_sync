name: "Cursor"
enabled: false

targets:
  agents:
    dest: ".cursor/AGENTS.md"

  rules:
    dest: ".cursor/rules"
    extension: ".mdc"
    header: "---\nglobs: '**/*'\nalwaysApply: true\n---"

  skills:
    dest: ".cursor/skills"

  subagents:
    dest: ".cursor/agents"

  commands:
    dest: ".cursor/commands"

  mcp:
    source: ".ai/src/mcp/cursor.json"
    dest: ".cursor/mcp.json"

  hooks:
    source: ".ai/src/hooks/cursor.json"
    dest: ".cursor/hooks.json"
