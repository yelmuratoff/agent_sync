name: "Google Antigravity"
enabled: false

targets:
  agents:
    dest: "GEMINI.md"

  rules:
    dest: ".agent/rules"
    header: "---\ntrigger: always_on\n---"

  skills:
    dest: ".agent/skills"

  commands:
    dest: ".agent/workflows"
