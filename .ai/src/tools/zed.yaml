name: "Zed"
enabled: false

targets:
  rules:
    dest: ".rules"
    merge_to_file: true
    prepend_agents: true
  skills:
    inline_into_agents: true

  settings:
    source: ".ai/src/settings/zed.json"
    dest: ".zed/settings.json"
