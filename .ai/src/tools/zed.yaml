# Zed Editor Configuration
# All rules merged into single .rules file with AGENTS.md prepended
name: "Zed"
enabled: true

targets:
  rules:
    dest: ".rules"
    merge_to_file: true
    prepend_agents: true
