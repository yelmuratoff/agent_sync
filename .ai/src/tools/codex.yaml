# OpenAI Codex Configuration
# Codex reads AGENTS.md from project root, not from .codex/
name: "OpenAI Codex"
enabled: true

targets:
  agents:
    dest: "AGENTS.md"

  skills:
    dest: ".agents/skills"
