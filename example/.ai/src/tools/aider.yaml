# Aider Configuration
# All rules merged into single CONVENTIONS.md with AGENTS.md prepended
name: "Aider"
enabled: true

targets:
  rules:
    dest: "CONVENTIONS.md"
    merge_to_file: true
    prepend_agents: true
