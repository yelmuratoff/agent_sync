# Tabnine Configuration
# 00-context.md contains AGENTS.md so Tabnine knows agent identity
name: "Tabnine"
enabled: true

targets:
  agents:
    dest: ".tabnine/guidelines/00-context.md"

  rules:
    dest: ".tabnine/guidelines"
