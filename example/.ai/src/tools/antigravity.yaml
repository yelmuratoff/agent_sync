# Google Antigravity Configuration
name: "Google Antigravity"
enabled: true

targets:
  agents:
    dest: "GEMINI.md"

  rules:
    dest: ".agents/rules"
    header: "---\ntrigger: always_on\n---"

  skills:
    dest: ".agents/skills"
