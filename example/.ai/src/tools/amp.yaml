# Amp (Sourcegraph) Configuration
# Amp reads AGENTS.md from project root
# Rules are inlined into AGENTS.md (Amp has no separate rules directory)
name: "Amp"
enabled: true

targets:
  agents:
    dest: "AGENTS.md"

  rules:
    inline_into_agents: true

  skills:
    dest: ".agents/skills"
